# vim: ft=sls

mine_functions:
  docker.manager_token:
    - mine_function: cmd.run
    - 'docker swarm join-token manager -q'
  docker.manager_ip:
    - mine_function: network.ip_addrs
    - {{ swarm_interface }}
  docker.worker_token:
    - mine_function: cmd.run
    - 'docker swarm join-token worker -q'
